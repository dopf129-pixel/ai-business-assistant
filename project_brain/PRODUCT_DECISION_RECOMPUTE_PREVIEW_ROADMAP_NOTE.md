# Roadmap Note — Product Decision Recompute Preview v1

Completed after v34 recompute authorization.

The system can now perform an explicitly authorized Product Decision recomputation as a non-persistent preview using the existing decision engine.

The preview does not replace or persist the current Product Decision, mutate drafts, call Ozon, invoke the legacy Action Executor, or enable execution.
