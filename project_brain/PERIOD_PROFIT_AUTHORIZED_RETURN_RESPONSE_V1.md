# Period Profit Authorized Return Response v1

Authorized return-financial evidence is now visible in the user-facing period profit response.

The response shows matched operation count, matched amount and mapping ID only when an authorized mapping artifact was actually applied.

Matched return operations are explicitly described as already included in `net_accrual`; they are never subtracted from profit a second time.
