# Roadmap Note — Product Decision Preview Review v1

Completed after v36 Product Decision Preview Delta.

A changed recompute preview can now receive an explicit ACCEPT / REJECT review result.

Acceptance remains non-persistent: it does not replace or save the Product Decision, mutate drafts, call Ozon, invoke the legacy Action Executor, or enable execution.
